import { CostoDineroPipe } from './costo-dinero.pipe';

describe('CostoDineroPipe', () => {
  it('create an instance', () => {
    const pipe = new CostoDineroPipe();
    expect(pipe).toBeTruthy();
  });
});
